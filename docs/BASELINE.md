# Baseline перед Stage 1

Дата аудита: 21.08.2026

## Исходный архив

Файл: `TurBox.zip`

SHA-256:

`205c12af3d31d0d9ca085f233e57ad4ee4d726c2fda26e8623e0d71c1f7b9eeb`

## Git исходного проекта

Последний commit исходной истории:

`5145095 feat: Рефакторинг агента Grok. Добавлен режим для отеля со сменой городов`

На момент загрузки working tree не совпадал с commit: основные Python-файлы и конфиги были изменены, а четыре реально используемых BAT-файла были untracked. Поэтому Git не являлся надёжной копией фактически используемой рабочей версии.

Кроме того, `configs/travelpayoutsSetup.txt` был tracked в старой Git-истории, хотя уже находился в `.gitignore`. По этой причине Stage 1 создаёт новую чистую Git-историю без credentials.

## Рабочее доказательство baseline

Успешный результат перед рефакторингом:

`postsCollections/collection_2026-08-19_21-30-42.txt`

Его копия сохранена в:

`samples/golden/collection_2026-08-19_21-30-42.txt`

## Размер основных файлов до Stage 1

- `collection_url_generator.py`: 1227 строк;
- `collection_link_converter.py`: 477 строк;
- `link_converter.py`: 456 строк;
- `browser.py`: 103 строки.

## Основной принцип baseline

Если структурное изменение нельзя подтвердить unit-тестом или живым smoke-test на том же пользовательском workflow, оно не считается безопасным и переносится на следующий этап.
