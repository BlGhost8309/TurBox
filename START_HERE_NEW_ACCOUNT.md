# START HERE — если проект открыт в новом ChatGPT-аккаунте

Загрузи весь этот ZIP в новый чат и напиши примерно:

> Это актуальный handoff проекта TurBox. Сначала внимательно прочитай `HANDOFF.md`, `README.md`, `docs/NEXT_STEPS.md` и `docs/TEST_RESULTS_2026-08-22.md`. Не начинай переписывать проект целиком. Продолжи с P0: оптимизация `cheapest_date` в `collection_url_generator.py` с сохранением текущего поведения и сравнением таймингов с baseline.

## Главное в одном абзаце

Stage 1 уже завершён и проверен. Travelpayouts converter на smoke работает. Основной OnlineTours generator полностью прошёл 57 запросов, но был медленным (~60.5 минут). После точечного performance patch пять запросов выполняются в среднем ~42.8 секунды; выбор города уже ускорен. Следующий bottleneck — `cheapest_date`, занимающий ~16.6–21.9 секунды. Следующая работа должна быть точечной именно там. Параллельно позже надо правильно различить `NO_RESULTS` и `PRICE_PARSE_ERROR` в шести случаях отсутствующей цены.

## Не потерять

В ZIP нет реальных Travelpayouts credentials/cookies. Они должны оставаться только локально у владельца.
